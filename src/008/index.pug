doctype html
html
  head
    append css
      link(rel="stylesheet" href="./index.css")
    title 008
  body
    .frame
      .center
        .circle
        - for (var i = 1; i <= 8; i++)
          div(class=`blubb-${i}`)
        - for (var i = 1; i <= 10; i++)
          div(class=`sparkle-${i}`)